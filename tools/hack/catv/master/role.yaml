apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: catv-master
  namespace: catv
  labels:
    app: catv-master
rules:
  - verbs:
      - create
      - get
      - list
      - update
    apiGroups:
      - 'batch'
    resources:
      - jobs

{
  "type": "object",
  "required": [
    "type",
    "name"
  ],
  "properties": {
    "type": {
      "type": "string",
      "title": "Type",
      "default": "vertica",
      "readOnly": true
    },
    "name": {
      "type": "string",
      "title": "Name",
      "default": "vertica-01"
    },
    "data": {
      "type": "object",
      "required": [
        "host",
        "port",
        "database",
        "username",
        "password",
        "max_open_connection",
        "max_lifetime"
      ],
      "properties": {
        "host": {
          "type": "string",
          "title": "Host",
          "default": "localhost"
        },
        "port": {
          "type": "integer",
          "title": "Port",
          "default": 5433
        },
        "database": {
          "type": "string",
          "title": "Database",
          "default": "vertdb"
        },
        "username": {
          "type": "string",
          "title": "Username",
          "default": "dbadmin"
        },
        "password": {
          "type": "string",
          "title": "Password",
          "default": "dbadmin"
        },
        "max_open_connection": {
          "type": "integer",
          "title": "Max open connection",
          "default": 10
        },
        "max_lifetime": {
          "type": "integer",
          "title": "Max lifetime",
          "default": 14400
        }
      }
    }
  }
}